import React,{createContext} from 'react';
export let HomesContext = createContext([]);
function HomeContext(props) {
    return (
        <div>
            salom
        </div>
    );
}

export default HomeContext;